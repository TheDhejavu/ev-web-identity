import React, {Component} from "react";
import { Typography } from 'antd';

const { Title } = Typography;

class Identity extends Component {
	render() {
		return (
			<div className="">
                <Title level={2}>  Welcome To Digital Identity</Title>
			</div>
		);
	}
}

export default Identity